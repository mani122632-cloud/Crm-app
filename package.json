{
  "name": "crm-app",
  "version": "1.0.0",
  "private": true,
  "description": "CRM - Capacitor Android project with native notifications, contacts and calendar",
  "scripts": {
    "copy": "node scripts/copy-www.js",
    "cap:sync": "npm run copy && npx cap sync android",
    "cap:open": "npm run cap:sync && npx cap open android",
    "build:apk": "npm run cap:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor-community/contacts": "^6.0.1",
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0",
    "@capacitor/status-bar": "^6.0.1",
    "@ebarooni/capacitor-calendar": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}

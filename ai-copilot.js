// ai-copilot.js — NEW AI MODULE (Phase 1 — AI Copilot architecture).
// PURPOSE: reserved file for the future AI Copilot UI layer. This is an
// EMPTY SKELETON ONLY for Phase 1:
//   - No DOM elements are created.
//   - No route is registered (Routes.* is left untouched).
//   - No existing screen (customer360.js, dashboard.js, app.js, etc.)
//     is modified or hooked into.
//   - No call is made to AIService yet.
//
// A future phase will add UI rendering here and wire it to AIService,
// without editing any existing CRM file.
/* global AIService */
const AICopilot = {
  // Intentionally empty in Phase 1. Reserved for future UI methods,
  // e.g. renderCustomerSummaryPanel(customerId), renderNextActionButton(dealId).
};
